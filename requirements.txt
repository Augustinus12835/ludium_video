# Ludium Video — lecture-to-video pipeline (math + technical modes)

# Core utilities
requests>=2.28.0
python-dotenv>=1.0.0

# Text-to-speech + Scribe speech-to-text (the only paid API)
elevenlabs>=1.0.0
mutagen>=1.47.0           # MP3 duration/metadata reading

# YouTube as an input source
yt-dlp>=2025.1.1          # audio download for Scribe transcription
youtube-transcript-api>=1.0.0  # fetch captions when available

# Math verification & animation
sympy>=1.12               # SymPy assertions in math verification
manim>=0.19.0             # Manim Community Edition (frame rendering)

# Audio splicing + frame audit
numpy>=1.24.0
pillow>=9.0.0

# PowerPoint source decks
python-pptx>=1.0.0

# Optional: local Whisper transcription for short local videos
# (ElevenLabs Scribe is the default engine; install only if you want --whisper)
# openai-whisper>=20231117

# System-level dependencies (not pip-installable):
# - ffmpeg/ffprobe:  sudo apt install ffmpeg
# - LaTeX for Manim: sudo apt install texlive-latex-extra texlive-fonts-extra \
#                    texlive-science cm-super dvisvgm libpango1.0-dev
